//------------------//
//  Jukka Pirinen   //
//  AR modeling app //
//      2018        //
//------------------//
    //This is not anymore useful file?
    /*
using UnityEngine;

public class MoveObject : MonoBehaviour {

    float rotateSpeed = 20;
    //public float x = 0;
    //public float y = 0;
    //public float z = 200;

    /*void Update()
    {
        transform.Rotate(x, y, x);
    }
    */
    /*
    void OnMouseDrag()
    {
        float rotateX = Input.GetAxis("Mouse X") * rotateSpeed * Mathf.Deg2Rad;
        float rotateY = Input.GetAxis("Mouse Y") * rotateSpeed * Mathf.Deg2Rad;
        
        transform.RotateAround(Vector3.up, -rotateX);
        transform.RotateAround(Vector3.up, rotateY);
        //transform.RotateAround(Vector3.zero, Vector3.up, 20 * Time.deltaTime);
        //transform.Rotate(0, Time.deltaTime, 10, Space.World);
        //transform.RotateAround(renderer.bounds.center, new Vector3(1, -1, 0), rotateSpeed);

    }
}
*/